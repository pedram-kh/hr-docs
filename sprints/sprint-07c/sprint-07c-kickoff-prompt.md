# Sprint 7c — Cursor kickoff prompt (plan-gate)

> Paste into a **fresh** Cursor thread (the `hr-platform/` workspace). Inspect the real answer loop, confirm additivity, plan **both phases**, and **stop** — no answer-path code until the plan is reviewed. **This touches the frozen 2b loop; the plan must prove the change is additive and keep Phase 1 separable/shippable as the fallback.**

---

You are in the `hr-platform` workspace. Sprints 0–7b are built and committed (the answer engine + the full Structured Reference Knowledge type — substrate, manual path, AI segmentation agent). **This is Sprint 7c — the multi-source composition layer**, the ONE sprint that touches the frozen 2b answer loop. Read `roadmap.md` Sprint 7c and `hr-docs/sprints/sprint-07c/spec.md`.

7c makes verified reference facts **answerable** and **composable with convenio prose**, built as **one sprint in two internal phases under one review**: **Phase 1 — the routed reference-fact answer** (mirrors the salary path exactly; proven, additive, regression-checked; lands first and clean). **Phase 2 — the fact+convenio-prose merge** (composes one grounded answer, convenio governing; builds on top). **Foundation-first is deliberate:** Phase 1 must be committed-clean and independently shippable, so if Phase 2's merge proves too delicate it defers cleanly rather than leaving a half-built tangle.

Before anything, read in full and cite real lines:
- **The 2b answer loop** in `architecture.md`: the **router** (`/route`, ADR-0016) + the **deterministic salary pre-classifier** in `RouterService` (the no-LLM short-circuit — Phase 1's pre-check mirrors it); **3a `SalaryAnswerService`** (SQL, ADR-0006 — scope+as-of resolution, the **exact-cell answer**, the **`chunk_id=null` citation**, **skip `/ground`**, **`salary_coverage_gap`**); **4 `/synthesise`** (ADR-0015 — abstains-not-fabricates, substantive-vs-provenance); **5 the answer-or-escalate decision** (Check A/B, `floor_decision`, `path:"salary_sql"`); the **authority-precedence rule** + the **widened-pool re-rank**; the **per-claim grounding gate** `/ground`; the **salary citation** (`message_citations.chunk_id = null`).
- **`SalaryAnswerService`, `RouterService`, `ChatService`** (the actual classes) — how salary plugs into the turn pipeline, so the reference-fact branch plugs in **the same way**.
- **`reference_facts`** in `data-model.md` + the model — the columns 7c queries: `convenio_id`, `topic_id`, `job_category_id`, `group_label`, `validity_start/end`, `value`, `raw_values`, **`status`** (only `verified` answers), `authority_level` (bounded `structured_reference` — ADR-0021), `source_document_id`. Confirm the verified-fact query is a clean parallel to the salary-table query.
- The **topic lexicon/anchor** the precedence re-rank already uses (vacaciones, jornada, permisos, periodo de prueba, …) — the deterministic topic match the pre-check reuses (no new classifier).
- ADR-0006/0015/0016/0020/0021.

Your task this turn: **inspect the real loop, confirm additivity for both phases, and plan — write no answer-path code.**

Produce `hr-docs/sprints/sprint-07c/plan.md`, then **STOP and wait for review.** Cover:

1. **What exists (the salary precedent).** The real `RouterService` salary pre-classifier, `SalaryAnswerService` (scope+as-of, exact-cell, `chunk_id=null`, skip-`/ground`, `salary_coverage_gap`), the answer-or-escalate decision + `salary` trace block + `floor_decision.path:"salary_sql"`, the precedence rule, `/synthesise`, `/ground`. Show real lines. **Confirm the reference-fact path is a clean parallel beside these, touching none of their internals.**
2. **Phase 1 — the routed reference-fact answer.** The deterministic pre-check (the salary-pre-classifier slot; topic-match via the **existing lexicon**, no LLM; fail-safe → fall through); `ReferenceFactAnswerService` (the verified-in-scope-in-validity query; the `value`+`raw_values` answer; the `chunk_id=null` citation; `authority_used=structured_reference`; **skip `/ground`**; `reference_fact_coverage_gap`; the `reference_fact` trace block + `floor_decision.path:"reference_fact"`); the two-verified-match safe rule (most-recent validity else escalate; resolution is 7d). **State clearly that Phase 1 is independently shippable.**
3. **Phase 2 — the fact+convenio-prose merge.** Detecting a composition turn (fact present **and** governing convenio prose present, via the existing prose retrieval); composing **one grounded answer** by handing **both** sources to `/synthesise` as **typed, authority-labelled sources** (fact at `structured_reference`, convenio at `official_convenio`, ordered by the **existing** precedence rule), **the convenio governing**; same-point conflict → **escalate, never blend**; **the composed answer goes through `/ground`** (each substantive claim entailed against its cited source; citation set = fact `chunk_id=null` + convenio chunks). **The line to draw explicitly: Phase 1 quotes a verified value (skip ground); Phase 2 generates a composed answer (must ground).** `/synthesise` and `/ground` are **reused, not rewritten** — the fact is an *added typed source*.
4. **The additivity proof + the regression check (the load-bearing section).** Exactly what changes and what does **not**. Prove the prose path (`/retrieve`·`/synthesise`·`/ground`·Check A/B·re-rank) and the salary path are **untouched** for non-reference-fact turns, **after both phases**. Propose the **regression check** (a representative prose question and a salary question → identical answers/traces to pre-7c). **State how Phase 1 stays separable** (committed-clean before Phase 2 begins; the deferral fallback is real).
5. **Migrations & build order** — **strict: Phase 1 fully built + regression-clean + (recommended) committed before Phase 2 begins.** Across `hr-backend` (the pre-check, `ReferenceFactAnswerService`, the trace/floor_decision additions, the new escalation reason, the citation wiring; then Phase 2's composition-detection + the typed-source synthesis wiring + the grounding citation set), `hr-frontend` (surfacing the fact answer + its source citation — reuse the salary-citation rendering; then the composed answer + multi-source citations), `hr-ai` (Phase 1: **none** — DB query + answer assembly like salary; Phase 2: **no rewrite** — `/synthesise`/`/ground` receive the fact as an added typed source; confirm whether the request shapes already carry an authority-labelled source list or need an additive field). List every additive migration (likely only an enum add for the new reason). Flag the **ADR**.
6. **Assumptions & open questions** — esp. the topic-match mechanism, group/job_category vs convenio-wide fact resolution, validity/as-of selection, the reference-fact-vs-salary ordering, composition-turn detection, the skip-ground (P1) vs ground (P2) boundary, how the fact is passed as a typed source to `/synthesise`/`/ground` (additive field?), whether the new escalation reason needs a migration, and anything the real salary/synthesis code makes non-obvious.

Hard constraints:
- **Purely ADDITIVE — the frozen 2b loop discipline.** Prose and salary turns must be **byte-for-byte unchanged** vs today, **after both phases**. The reference-fact path is a **new branch beside salary**; Phase 2 **reuses** `/synthesise`/`/ground` by adding the fact as a typed source — it never rewrites them or the salary/prose internals. If making facts answerable/composable seems to require changing those internals, **stop and flag**.
- **Phase 1 first, separable, shippable.** Build + regression-check + (recommended) commit Phase 1 before Phase 2. Phase 2 must be deferrable without leaving Phase 1 half-built.
- **Only `verified` facts answer** (ADR-0020/0021 gate lifts exclusively for `verified`) — **the safety spine of all 7b connecting to chat; test it hardest.**
- **Precedence preserved — the convenio always governs.** `structured_reference` below `official_convenio`; a fact never overrides the law; same-point conflict escalates, never blends.
- **Phase 1 skips `/ground` (quoted verified value); Phase 2 must `/ground` (synthesized composition).** Draw the line explicitly.
- **`hr-backend` owns the deterministic decision** (ADR-0007/0015/0016); additive migrations only.
- Reuse the **salary routed-path pattern** + the **salary citation rendering** + the existing **topic lexicon** + `/synthesise`/`/ground`/the precedence re-rank + the `floor_decision`/trace shapes.

Do not create or modify any file other than `hr-docs/sprints/sprint-07c/plan.md` this turn. After writing it, stop and say it is ready for review.
