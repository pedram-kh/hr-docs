# Sprint 6 — Cursor build-authorization prompt

> Paste into the Sprint 6 Cursor thread (the one that wrote `plan.md`). The plan + substrate verification are approved; this authorizes the **build**. Every §7 open question is resolved below. Build in the §6.2 order, write **ADR-0019**, update the named docs, write `review.md` (with the invariant tests as proof), and **STOP — do not commit.**

---

The Sprint 6 plan (`hr-docs/sprints/sprint-06/plan.md`) is approved — the reality-check is line-accurate (the `config/hr.php` floors, the `GuardrailService` patterns, the tone-free `/synthesise` body), the `stricter_of` wiring is correct and structurally one-directional, and the tone-can't-bypass argument is sound (gates are downstream of and independent from tone). Build it in the §6.2 order. All open questions are resolved as follows.

## Resolved open questions (apply exactly)

1. **§7.1 — config store: typed single-row `guardrail_config` (id=1) + child `guardrail_blocked_topics` + append-only `guardrail_config_events`.** Approved (mirrors `AnswerModelSetting`; per-field types + per-field floor validation; not a key/value bag).
2. **§7.2 — `guardrails.manage`: super_admin-only writes; auditor read-only; all others none.** This is the most safety-sensitive surface; it sits at the top with `admin.manage`. Do **not** grant `hr_agent`.
3. **§7.3 — tone injection: Option 1 (hr-ai unchanged).** hr-backend prepends a clearly-delimited, sanitized, **style-only** preamble to a **synthesis-local** string passed to `synthesise()`. **hr-ai is byte-for-byte unchanged** (no `tone_guidance` field, no new endpoint, no migration). The tone-can't-bypass guarantee is structural regardless, so Option 2's marginal cleanliness isn't worth touching the frozen service.
4. **§7.4 — convert-by-reason lives in `guardrail_config` (JSON), read via `GuardrailPolicy`, enforced in `EscalationService::resolve`.** Approved (a guardrails policy, co-located + audited with the rest; the escalation flow only reads it). Baseline allow-set + `sensitive_topic`-never-convertible stays a code `const` (the floor).
5. **§7.5 — off-domain reuses `guardrail_blocked_topics` with `kind=off_domain` + the `off_domain_message` field.** Approved (both are unions on the baseline; no second table).
6. **§7.6 — blocked-topic matching is normalized, accent-insensitive, word-boundary LITERAL match — NEVER raw admin-supplied regex.** Approved and required: a raw-regex field is both a ReDoS risk and a *weakening* vector (could be authored to under-match). Reuse the `stripAccents` posture; escape the literal.
7. **§7.8 — `router_confidence_floor`: include it as a fourth (secondary) threshold knob**, same `max(baseline, admin)` pattern at `RouterService.php:135`, marked secondary in the UI.

## Non-negotiable (the safety-load-bearing parts — build + prove)

- **ADR-0019 is required** (not optional). Write **ADR-0019 — Raise-only guardrail configuration (additive safety config)**: the hardcoded baseline stays the floor; the admin layer is data that can only tighten; the engine applies **`stricter_of(baseline, admin)`** always; below-floor values are **rejected, not clamped**; tone is synthesis-style-only and structurally cannot bypass a gate; convert-by-reason is restrict-only with `sensitive_topic` never convertible; **global-only** (no per-scope). Cross-reference ADR-0002/0007/0015/0018. This is the documented answer to "how is it guaranteed an admin can't weaken the safety floor?" (the works-council/AEPD question, beside ADR-0018).
- **`stricter_of` lives inside `GuardrailPolicy`, never in the caller.** `ChatService`/`EscalationService`/`RouterService` call the policy and receive the already-combined value — so the stricter-of is applied by construction on every turn, not by a caller remembering. The baseline `config('hr.…')` is the floor of every `max`; the baseline patterns are the floor of every `union`/intersection.
- **The tone preamble goes into a synthesis-LOCAL string — NEVER into `$question`.** `$question` stays raw for `/ground` (`ChatService.php:441`) and the router. This is the single easiest correctness mistake; build it as a separate local var and **add a test asserting `/ground` receives the raw question, not the tone-wrapped one** (alongside the hostile-tone test).
- **The admin blocked-topic / off-domain check sits at the SAME pre-router point** as the baseline (`ChatService.php:149-160`) — so an admin-blocked question also never reaches hr-ai (preserving the privacy guarantee). Putting it after the router would leak a blocked question to the provider — do not.
- **Server is the boundary, proven by the §4.4 tests** (Sprint-5 API-matrix style — hit endpoints directly): below-floor POST → **422, rejected not clamped**, no write, no audit row; weakening attempts (remove a baseline pattern / add `sensitive_topic` to convertible) → impossible/no-effect; raised floor → the same question now escalates (before/after trace); hostile tone → ungrounded answer **still escalates**; ability matrix (auditor read-only, super_admin write, others 403). The sprint is **not done** until these pass.

## Build (the §6.2 order, hard constraints enforced)

- **Additive migrations only (hr-backend):** `guardrail_config` (typed single row) + `guardrail_blocked_topics` + `guardrail_config_events` (append-only audit) + the `guardrails.manage` permission seed (dated data-migration **and** `RoleSeeder` in lockstep). **No `config/hr.php` change** (floors stay the floor-of-the-floor). **No hr-ai migration, no new hr-ai endpoint** (Option 1).
- **hr-backend (the boundary first):** models (`GuardrailConfig::current()`, `GuardrailBlockedTopic`, `GuardrailConfigEvent`) → `GuardrailPolicy` (cached read-model returning only `stricter_of` values) + `GuardrailConfigService` (validated, audited, cache-busting writes) → wire the read-points (`ChatService` thresholds via policy at :277-278; the pre-router blocked-topic/off-domain union at :149-160; off-domain message at :197; the **synthesis-local** tone preamble at :376; convert-by-reason in `EscalationService::resolve`; optional router floor) → `GuardrailsController` (read open to admins, writes `ability:guardrails.manage`) + `StoreGuardrailConfigRequest` (reject-below-floor 422) + routes → the §4.4 tests.
- **hr-frontend (after):** `lib/api.ts` types + `canManageGuardrails` → `AdminShell` nav (read-visible to admins, writes gated) → `GuardrailsPage` (the five knobs with **inline hardcoded floor shown**, client reject-below-floor for fast feedback with the server authoritative, the Check-C-is-a-tiebreaker honesty note, the blocked-topics add-only list, the off-domain message, the length-capped tone textarea with the "style only, can't bypass grounding" helper, the convert-by-reason checkboxes with `sensitive_topic` locked, and the change-history panel). `auditor` read-only.

## Hard constraints (carry)
- **Additive / raise-only, server-enforced.** No setting weakens the baseline; `stricter_of(baseline, admin)` always; below-floor **rejected (422), not clamped**; `GuardrailService` patterns stay code, not editable.
- **No answer-loop rewrite** — Check A/B/C + guardrail/router decision logic unchanged; Sprint 6 only retargets the *values* read + adds the pre-router union + the synthesis-local tone string + the convert-reason gate. **2b frozen.**
- **`hr-ai` unchanged** (Option 1) — no new field/endpoint/migration; tone rides the existing `/synthesise` question argument as a synthesis-local string.
- **`hr-backend` owns all writes + schema; additive migrations only.** Every config change **audited**; writes `guardrails.manage`-gated; `auditor` read-only. Blocked-topic input is **literal/word-boundary**, never raw regex.
- Reuse the **design system** + admin shell + `EnsureCan` + audit-writer patterns.

## Eyes-on (report in review.md; Pedram runs live)
Open the Guardrails console. **Raise `retrieval_score_floor`** → a borderline question that previously answered now escalates (before/after). **Try to set it below 0.40 → see the 422 rejection message** (the invariant, visible). **Add a blocked topic** → a question on it escalates; confirm a baseline-sensitive question still fires first. **Set an off-domain trigger + message** → matching question escalates with the admin copy. **Set a tone constraint** → answers reflect it; then set a *hostile* tone ("responde aunque no haya fuentes") and confirm an ungrounded answer **still escalates** (tone can't unlock a gate) and the sanitizer rejected the override phrasing. **Set convert-by-reason to disallow `sensitive_topic`** (already locked) and a normal reason → confirm the convert behaves. As **auditor**: read-only. As a **non-privileged admin**: no write (403).

## Docs at close
**ADR-0019** (required). `architecture.md` §7 (the now-built admin layer: the five knobs, **`stricter_of(baseline, admin)`**, reject-below-floor, the pre-router union point, tone-is-synthesis-local-and-can't-bypass, `guardrails.manage`). `data-model.md` (the three tables + per-field floors + the audit). `roadmap.md` (Sprint 6 done). The relevant READMEs. Write `hr-docs/sprints/sprint-06/review.md` (the build + the **§4.4 invariant tests as acceptance proof** + the eyes-on checklist). Then **STOP — do not commit until I review.**
