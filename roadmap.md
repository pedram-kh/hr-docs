# HR Platform — Roadmap

> Canonical location: `hr-docs/roadmap.md`
> Status: **living document.** This is the full, sequenced scope of the project. It **supersedes the high-level build order in `architecture.md §12`** (which predates the Sprint 1 split). When they disagree, this file wins; `architecture.md §12` should be reduced to a pointer here.
> Read with `architecture.md` (the *what* and *why*), `data-model.md` (the schema), and the `decisions/` ADRs.

This document exists because the full scope nearly got lost to a chat compaction. It is the durable, countable answer to "how big is this project" — so the scope lives in the repo, not in anyone's memory.

---

## 1. Where we are

| Sprint | Scope | Status |
|---|---|---|
| **Sprint 0** | Foundation & walking skeleton (3 services, 24-table schema + pgvector, email-OTP login into empty shells) | ✅ **committed** |
| **Sprint 1** | Knowledge-ingestion backbone (territories restructure, registry import, PDF ingestion via `hr-ai /extract`, deterministic filename parser, conflict detection, tag provenance, basic admin documents table) | ✅ **committed** (spec, plan, build, review, corrections 01–02) |
| **Design-system adoption** | Vanilla-CSS token system, light-default + dark toggle, Inter, two existing screens refactored (ADR-0012) | 🔄 **built; pending real-app eyes-on + commit** |
| **Sprint 2a** | Ingestion-to-vectors: article-aware bilingual chunking, BGE-M3 / `vector(1024)` into `document_chunks`, structured salary-row extraction, the scope-prefilter `/retrieve` primitive + `retrieval:probe` harness | ✅ **built** (pending final commit) |
| **Sprint 2b-1** | The narrow answer vertical: `hr-ai /synthesise` (pluggable Claude) with the **authority-precedence** rule, `hr-backend` pipeline + answer-or-escalate floor + hardcoded guardrail baseline + external-key handling (ADR-0015), chat UI + admin "Answer model" screen, citations + trace + history | 🔄 **built; pending eyes-on (real key) + commit** |
| **Sprint 2b-2** | Completes the answer surface: the **router** (ADR-0016, `/route`), **salary-in-chat** (SQL, exact + year-aligned), **single-turn constrained category pick**, the **full per-claim grounding gate** (`/ground`), **recall hardening** (sub-query union + national-law pass), citation-numbering fix, `router_decision`/grounding/salary trace blocks, `salary_coverage_gap` reason | 🔄 **built; pending eyes-on (real key) + commit** |
| **Sprint 2c** | **Article-boundary re-chunk** (substrate; ADR-0017): one chunk per article, cross-article packing removed (the durable buried-grant fix), load-bearing detector with three precision guards (line-anchored, case-aware, monotonic-number), cap locked 512/800 on the real BGE-M3 distribution, idempotent re-embed of the registry-active convenio set; **Correction-03 residual closed** (the precedence re-rank reclassified to defence-in-depth). Estatuto (id 73) held out → Sprint 8 follow-up | 🔄 **built; pending eyes-on (real key) + commit** |
| **Sprint 3** | Knowledge Center (lens-hierarchy admin UI): the reusable hierarchy (graph + list, lazy), coverage-gap markers, the full document card (chunk health, lineage, real-PDF viewer, read-only sandbox), bounded edit with append-only provenance + scope-gate, `knowledge.edit` role gating, additive read-only hr-ai `/sandbox-retrieve`. No migration | 🔄 **built; pending eyes-on + commit** |
| **Sprint 4** | Escalation board + the flywheel: the Kanban board (read open, write `escalation.work`-gated, all actions → `escalation_events`), two-way chat (`hr_agent` author kind + employee `GET /chat/session` hydrate/poll), Save-as-knowledge (scope-confirm + publish-time no-silent-override conflict fence), `internal_hr_ruling` made retrievable via the existing render→`/extract`+`/embed` path (hr-ai untouched, lossless round-trip verified), Knowledge-Center badge + provenance back-link. Two additive migrations | 🔄 **built; pending eyes-on + commit** |
| **Sprint 5** | User & directory management + roles + role-scoped history (the access-control layer, pillar §4.2; **ADR-0018**): employee directory CRUD/search + CSV bootstrap (validate→report→apply, per-row, audited) + the now-written `employee_audit_log` + email-change 409; admin/role management via spatie (the four roles UI-assignable) + status enforcement (both OTP paths + token revocation + `EnsureActiveAccount`); the new **`history.view_all`** ability + the gated full-history browser/search + the append-only **`conversation_access_log`** (every access logged, incl. super_admin) + the `knowledge_editor` card-payload tightening. Server-side enforcement proven by API-matrix tests. Two additive migrations | 🔄 **built; pending eyes-on + commit** |
| **Sprint 6** | Guardrails configuration UI (the admin layer over the hardcoded baseline; **ADR-0019**): five **additive, raise-only** knobs — retrieval/confidence/router thresholds (`max(baseline, admin)`), add-only blocked-topic/off-domain union (literal/word-boundary match, pre-router), off-domain message, synthesis-style-only tone, restrict-only convert-by-reason (`sensitive_topic` never). The combination math lives in one read-model (`GuardrailPolicy`, `stricter_of`); below-floor **rejected (422), not clamped**; writes `guardrails.manage`-gated (super_admin), reads open (auditor read-only), every change audited to `guardrail_config_events`. hr-ai unchanged; three additive migrations; invariant proven by API-matrix tests | 🔄 **built; pending eyes-on + commit** |

Sprint 2b is **split into two slices** (an honest-scoping split, like 1→1+corrections and RAG→2a/2b): **2b-1** is the single prose answer path; **2b-2** adds the router, full per-claim grounding check, salary-in-chat, and recall hardening. **Sprint 2b is now complete** (history-search UI moved to Sprint 5 with the rest of role-scoped history). Everything below is **ahead of us**.

---

## 2. How to read the sequence

- **Sizes** are rough and relative (S / M / L), not time estimates — we don't estimate in time, and sprints split when honest scoping reveals they're overloaded (Sprint 1 became 1 + two corrections; RAG splits into 2a/2b below). **The count grows as we get more honest, and that is the discipline working, not scope creep.**
- **Three pillars are cross-cutting, not single sprints** — they are built *into* other sprints and called out in §4 so they're never mistaken for one-off features: **auditable history**, **privacy/GDPR**, and **the learning-loop/flywheel**.
- The **LLM tagging tier stays late** — the deterministic parser already covers the clean majority, so the system ships value without it (ADR-0011 foundations are already in place from Sprint 1).

---

## 3. The forward sequence

### Sprint 2a — Ingestion-to-vectors  · size **L**
Turn ingested documents into retrievable knowledge.
- Chunking strategy for prose convenios + the Estatuto (article-aware; **never blend Euskara + Spanish into one chunk** — bilingual docs kept clean; ADR-0006).
- Embeddings: **BGE-M3 / `vector(1024)`** written into `document_chunks` by `hr-ai`, with the denormalised scope columns populated for pre-filtering.
- **Structured salary-row extraction** from the `Tablas` PDFs into `salary_tables` / `salary_table_rows` — so salary questions become **SQL lookups, never vector matches** (ADR-0006). This also populates `convenio_job_categories` (deferred from Sprint 1).
  - A third knowledge class — **Structured Reference Knowledge** (AI-segmented, human-verified, non-vectorized) — is defined in Sprint 7; it generalizes the salary "structured scoped facts, queried-not-vectorized" pattern to non-salary compiled/multi-scope sources (`.docx`/`.xlsx`).
- The scope-prefilter query path: given a resolved scope, return the eligible chunk/row set.
- Historical/`draft` documents excluded from current retrieval; `active` only.
- **Out:** the chat surface, the answer LLM, the router (that's 2b).

### Sprint 2b — Scoped RAG chat + answer loop  · size **L**  · *split into 2b-1 + 2b-2*
The first surface a real employee uses. Split into two slices so the answer vertical proves end-to-end before the router/grounding complexity lands.

**Sprint 2b-1 — the narrow answer vertical (built).**
- The employee **chat UI**, built fresh on the design system (proves the design language on a from-scratch screen): message list, input, answer + citations + expandable **"how I got here" trace**, escalation state via the signature badges.
- The **single prose path, no router** (`architecture.md §5`): deterministic **scope resolver** (`hr-backend`) → **hardcoded guardrail baseline** (fires before any `hr-ai` call) → **pre-filtered vector search** (2a `/retrieve`) → **cited synthesis** (`hr-ai /synthesise`, pluggable Claude) → **answer-or-escalate floor** (`hr-backend`) → answer **or escalate**.
- **Authority precedence in synthesis** (legal-weight): convenio chunks ordered before `national_law`, the precedence rule encoded in the prompt, `authority_used` recorded in the trace — the convenio governs where it speaks; the Estatuto is the baseline only where it is silent (ADR-0015).
- **External answer model + key handling** (ADR-0015): pluggable provider in `hr-ai`; the admin "Answer model" screen sets the key once, encrypted at rest, masked, rotatable, never read back; the key is passed decrypted per call, never reaches the browser.
- **Citations** back to source document + page (uncited answers never reach the employee); **the structured trace** written per turn (pillar §4.1); full **chat history** stored, role-scoping-ready, auditable.
- **Hardcoded guardrail baseline** lands here (no legal/medical advice, sensitive-topic auto-escalation) — the *config UI* for additive rules is later (Sprint 6).

**Sprint 2b-2 — the router + full grounding (built).**
- The **router** (small LLM, ADR-0016, `hr-ai /route`): salary vs prose vs off-domain, fail-safe, after the guardrail baseline. A deterministic salary pre-classifier (moved from `GuardrailService`) short-circuits obvious salary with no LLM call.
- **Salary-in-chat** (the 2a salary SQL surfaced through the pipeline; `SalaryAnswerService`). **Superseded** the 2b-1 *salary-topic guard* (Correction-02): salary questions are answered from `salary_tables` via SQL (exact, year-aligned), with a **single-turn constrained category pick** when the profile category is absent; a genuine coverage gap escalates the renamed **`salary_coverage_gap`** (`salary_not_in_chat` retired).
- The **full per-claim grounding gate** (`hr-ai /ground`, capable answer model, table-aware) — the real gate, which **demoted** 2b-1's *figure-grounding guard* (Correction-01) to a fast pre-check.
- **Recall hardening** — sub-query decomposition union + a national-law-only pass in `hr-backend` (the Q10 + silent-convenio recall fixes); `/retrieve` unchanged.
- **Parked — agentic multi-turn clarifier:** the salary category disambiguation is **single-turn** (a constrained pick). A fuller multi-turn clarifier (free-form back-and-forth to pin down ambiguous scope) is deferred — see §7.
- **Parked — 2a wage-table-chunk cleanup (still parked):** convenio PDFs ingested in 2a embedded their wage-table pages as prose `document_chunks`. Salary now routes to SQL (not those chunks), and the table-aware grounding gate rejects an answer that leans on digit-presence in a tabular chunk — but the deeper fix (excluding wage-table pages from prose chunking at ingest) remains undone.
- **Moved to Sprint 5 — history search** UI (lands with role-scoped history access).

### Sprint 3 — Lens-hierarchy admin UI  · size **L**  · **DONE**
The rich Knowledge-Center surface deferred from the original Sprint 1. Shipped as **Knowledge → Map**.
- The **one reusable hierarchy component** driven by a lens definition (ADR-0001): **branching-graph** (hand-rolled SVG, no layout dependency) and **indented-list** forms, two-level + lazy, expand/collapse, leaf-opens-card.
- The **lenses**: by territory, by sector, by validity, by topic.
- The full **document card**: facet tags with inline provenance, validity, retrieval status, authority level, version lineage, chunk health, the change-log timeline, the real-PDF viewer, and the read-only **"test a question against this document"** sandbox.
- **Coverage gaps** (deploy.md §5) surfaced as map markers, derived from existing data; a staleness signal kept distinct from a real hole.
- **Bounded edit**: FK pickers into existing vocabulary only (no restructuring — that stays in `registry:import`, ADR-0011), append-only `admin_manual` provenance, a scope-affecting confirmation gate. First writer of `document_topics`.
- **Roles**: `knowledge.edit` ability (super_admin + knowledge_editor) gates writes; reads (incl. the sandbox) open to any admin (auditor browses + inspects, cannot edit).
- This is the admin-side payoff of the faceted model built in Sprint 1.
- A third knowledge class — **Structured Reference Knowledge** (AI-segmented, human-verified, non-vectorized) — is defined in Sprint 7; it generalizes the salary "structured scoped facts, queried-not-vectorized" pattern to non-salary compiled/multi-scope sources (`.docx`/`.xlsx`). Its records appear here as distinctly-badged leaves in the lens hierarchy.
- **Deferred from here:** AI topic *proposal* + `ai_agent` provenance → **Sprint 7** (the reserved timeline-dot color is in place); `document_chunks` es/eu **language split** → deferred (out-of-scope hr-ai change; chunk-health omits it honestly); full **role/permission + directory management** → **Sprint 5**.

### Sprint 4 — Escalation board + the flywheel  · size **L**  · **DONE**
The Kanban board **and** the core of the learning loop (pillar §4.3).
- The **board**: cards auto-created on agent-fail / sensitive topic / off-domain / user-request, each carrying the full conversation + trace; status **New → Assigned → In Progress → Resolved**. Reads open to any admin; assign/move/reply/resolve gated by the new **`escalation.work`** ability (super_admin + hr_agent; auditor read-only). Every action audited to **`escalation_events`** (append-only).
- **Two-way chat:** a human reply is stored as an **`hr_agent`** `chat_messages` turn (additive role + `author_admin_id`); the employee re-reads its own session via self-scoped `GET /chat/session` (hydrate + poll), attributed as "Recursos Humanos" (no PII). **Card-scoped viewing only** (no full-history browser — Sprint 5).
- On resolve, the **"turn this into a knowledge article"** flow: a new `documents` row, `authority_level = internal_hr_ruling`, that **inherits the asker's scope by default and forces scope confirmation** before publish; the no-override rule is **enforced at publish** (conservative scope+topic conflict block → re-escalate, never silently published). Made retrievable through the **existing** ingestion path (render→PDF→`/extract`+`/embed`) with **hr-ai untouched** (Q-A "A1"; lossless round-trip verified).
- Implicitly the **gap-analysis** surface.
- *Dependency:* uses the roles seeded in Sprint 0 for access; the full directory UI is Sprint 5 (test users suffice to assign cards until then).
- **Known boundary (Q-F):** the conflict fence runs at **publish** time. The **symmetric check** — publishing/editing an `official_convenio` should re-scan existing rulings for newly-introduced conflicts (and demote/re-escalate) — is **deferred to a later precedence/guardrails sprint** (largely latent until a self-service convenio-upload path exists; 2b retrieval precedence stays frozen). Also deferred: restricting `convert` by escalation reason → Sprint 6 guardrails policy.
- *The publish conflict fence was `topic-tag`-based here and gained its semantic pass in **Sprint 7d** (ADR-0024) — additively, as `existing OR semantic`, so this Sprint-4 fence still stands unchanged as the first term. It governs the `internal_hr_ruling` publish path only and never blocks normal document ingestion.*

### Sprint 5 — User & directory management + roles  · size **M**  · **DONE** (built; pending eyes-on + commit)
The first-class directory (ADR-0004 — no HRIS/AD sync) + the access-control layer (**ADR-0018**).
- Full **CRUD**: manual create / edit / search / filter **plus** CSV bulk upload (bootstrap — two-phase validate→report→apply, per-row, transactional, audited; bad rows reported, never dropped). FK pickers into existing vocabulary only (ADR-0011), convenio-scoped `job-categories`.
- The **profile-change audit log** (`employee_audit_log`) — **now written** by `EmployeeAuditLogger` (one row per field, in the same transaction) — what a profile said at the moment of any answer = dispute defense — and the `profile_last_reviewed_at` staleness signal (set by an explicit **mark-reviewed**; an edit does not bump it).
- The four **roles** wired through the UI (`super_admin`, `hr_agent`, `knowledge_editor` no-chat, `auditor` read-only) via `spatie/laravel-permission`, with the abilities `directory.manage` / `admin.manage` / **`history.view_all`** attached to roles. **Role-scoped conversation access (pillar §4.2, ADR-0018):** the gated **full-history browser/search** (beyond Sprint 4's card-scoped view), enforced **server-side** (`EnsureCan`, proven by API-matrix tests), with the append-only **`conversation_access_log`** (every access logged — incl. super_admin). The `knowledge_editor` card-payload read is tightened; the `hr_agent` card boundary is unchanged. History-search UI (moved from 2b) lands here.
- **Status enforcement:** both OTP paths refuse inactive accounts; deactivation revokes tokens + `EnsureActiveAccount` ends access immediately (a deactivated employee can't chat).
- Editing an email = changing how that person logs in: the UI warns **and** the server requires a `confirm_email_change` (409 otherwise).

### Sprint 6 — Guardrails configuration UI  · size **M**  · **DONE** (built; pending eyes-on + commit)
The admin layer on top of the hardcoded baseline (which shipped in 2b) — **additive, raise-only, server-enforced (ADR-0019).**
- Admin-defined, **additive-only** rules: the retrieval/confidence/router **thresholds** (raise-only), **blocked topics** + **off-domain** triggers (add-only union, literal/word-boundary match — never raw regex), the off-domain **message**, **tone** constraints (synthesis-style-only), and **convert-by-reason** (restrict-only; `sensitive_topic` never convertible).
- Admins can **add to / tighten but never weaken** the baseline: the engine applies `stricter_of(baseline, admin)` in one read-model (`GuardrailPolicy`), a below-floor value is **rejected (422), not clamped**, the `GuardrailService` patterns stay code, and tone **structurally** cannot bypass a gate. Writes gated by `guardrails.manage` (super_admin only); reads open (auditor read-only); every change audited to `guardrail_config_events`. Three additive migrations; hr-ai unchanged. Invariant proven by API-matrix tests (`Sprint6GuardrailInvariantTest`).

### Sprint 7 — overview (split into 7a–7g)
Sprint 7 was scoped as one entry, but it is a family of genuinely distinct subsystems, so it is split into sub-sprints to keep blast radius tight — one risk at a time, the way Sprint 2b split. Recommended order **7a → 7b → 7c → 7d → 7e → 7f → 7g** (dependency-driven: 7c needs 7b's facts to exist; **7d/7e/7f are independent and can be ordered by need** — 7f, the proper fix for the latent group-scope bug, gates group-scoped fact answering becoming live; 7g is the follow-on that makes 7f's stricter escalation behaviour usable). **All of 7a, 7b (7b-1/7b-2), 7c, 7d, 7e, 7f, and 7g are DONE — Sprint 7 is complete.** 7g was split out of 7f rather than absorbed into it: escalation explanations (7f makes the system escalate in more cases, on purpose; 7g makes those escalations explicable to the employee and fixable by HR, fixes three operational traps found along the way, and closes the deploy-credential gap flagged at 7f's close).

### Sprint 7a — Messy-tail document intelligence  · size **M** · **DONE** (ADR-0020)
The messy-tail intelligence, built on foundations already laid. **Built:** the LLM tagging tier (`POST /propose-tags` in hr-ai; `TagProposalService` + the queued `ProposeDocumentTags` auto-trigger on ingest in hr-backend), the propose-new-vocabulary flow (`vocabulary_proposals` + `vocabulary.approve`, variant→alias default), the expiry queue (`reviews:scan-expiry`) and the now-written `predecessor_document_id` lineage write-side, and the fuchsia `--provenance-ai` signal for unverified-AI surfaces. The two safety invariants (AI stays `under_review` + 0 chunks; AI never writes the FK columns) are enforced and proven by `Sprint7aTagProposalInvariantTest`. See `sprints/sprint-07a/review.md`. ~~**Carried to 7d:** AI *suggestion* of succession candidates (successor/coexisting-sibling/conflict by meaning) — 7a built only the human-confirmed lineage write-side; the semantic comparison that proposes candidates rides 7d's machinery.~~ **Delivered in 7d (ADR-0024):** the suggestion is an inert `ai_agent` proposal on the expiry task, and `successor` requires overlap **and** strictly-later validity; 7a's write-side is unchanged, so the predecessor is still never auto-retired.
- The **LLM tagging tier** for documents without clean filenames (resolved-escalation articles, ad-hoc uploads, scans) — reads content, **proposes** facets with confidence (ADR-0011 rescue path, using the `reason`/`raw_unmatched_values` fields already built in Sprint 1).
- The **propose-new-vocabulary** UI (agent proposes a value should exist → human approves; strong default "variant → alias", ADR-0011).
- The **expiry review queue** with successor lineage (a queue, not a popup) — surfacing documents approaching `validity_end` and confirming the successor handoff.
- *Succession is scope-based, never topic-based, and never automatic.* A new document supersedes an existing one only when it is a newer version of the same scope (same `convenio`) — not merely because it shares a topic. Two documents covering the same topic but different areas/scopes (e.g. Limpieza Navarra vs Limpieza Álava *vacaciones*) must both stay active — retiring one would silently delete live information for a population. (Today this is naturally protected: scope derives from the `convenio`, so different-`convenio` documents are never succession candidates. Confirmed by audit: nothing in current code auto-sets `retrieval_status = 'historical'` as a side-effect of another document's upload — it is set only by the document's own signals at ingest (`Antiguo` path / past `validity_end`), explicit admin lifecycle edit, or ruling publish.) When the Sprint-7 upload UI is built, preserve this: succession must be an explicit — or AI-proposed then human-confirmed — decision (AI may propose *successor / coexisting-sibling / conflict* by comparing meaning; a human confirms the *retire-or-coexist-or-escalate*), never an automatic consequence of ingest. *Corollary — lineage write-side is currently a stub:* `predecessor_document_id` exists in the schema and is displayed on the Sprint-3 card, but no code writes it (always `null`). Implementing the lineage write-side is part of this succession work — version history isn't actually tracked until it's built.
- *7a is the low-risk messy-tail unit* — all "AI proposes → human confirms," nothing answerable without approval, no answer-loop change. The natural first slice.

### Sprint 7b — Structured Reference Knowledge (the new knowledge type)  · size **L** · **split into 7b-1 / 7b-2**
7b builds the new non-vectorized knowledge type and its segment → review → display flow; it does **not** change the answer engine (that's 7c). Without 7c the facts exist and are reviewable but aren't yet composed into answers.

> **7b split (build order).**
> - **7b-1 — substrate + manual path · DONE (ADR-0021).** The `reference_facts` table (salary-patterned), the `reference_source` document_type + the docx/xlsx reader (hr-ai `POST /read-structured`, python-docx + openpyxl, content stored as display `document_pages`, never embedded), the **manual** create/verify path, and the Knowledge-Center display (a distinct-badged leaf + fact card + verify). The two safety invariants are built **and tested** (`Sprint7b1ReferenceFactInvariantTest`): **(1)** authority is structurally bounded — the column holds only `structured_reference` and the request rejects 422 (a fact can never outrank a convenio); **(2)** routing rides `document_type` — a `reference_source` file produces only `reference_facts` (zero `salary_table_rows`), and `salary:import` ignores it. The `ai_agent` source lane is **reserved but unwritten** (manual path is the only writer); the answer engine is **not** touched (a verified fact being not-yet-answerable is correct — that's 7c).
> - **7b-2 — the AI segmentation agent · DONE (ADR-0022).** The risky proposer, reusing 7a's propose-pattern verbatim: a `reference_source` ingest auto-dispatches `SegmentReferenceSource` → hr-ai `POST /segment-facts` (reads `document_pages` + a convenio-centric closed vocabulary, returns per-scope facts with confidence/uncertainty/source_excerpt, closed-set-validated; writes nothing) → `ReferenceFactProposalService` upserts as `ai_agent`/`needs_review` (fuchsia, the harder verify look) and flags obvious duplicates. The **logical key was extended** to `(convenio_id, topic_id, job_category_id, group_label, validity_start, validity_end)` — `group_label` added (additive) so a convenio's per-group facts don't collide on null `job_category_id`. The load-bearing cognition is the **header-carry contract** (re-derive TERRITORY→SECTOR→group from text, not the unreliable reader sections) + **binding scope to a real `convenio_id`** (never minting). Inherited invariants **re-proven, not rebuilt** (`Sprint7b2SegmentationInvariantTest`): inert lane, authority floor, **zero salary rows**, idempotent re-runs, agent-never-verifies, duplicate-flag-without-merge. Review UX is the safety (uncertain-first queue, inline source line, reject + `rejected` status). **The deliverable is the measured accuracy report** (`sprint-07b-2/review.md`) against the three real fixtures, scored by a reproducible gold-set scorer (`sprint-07b-2/eval/`). Duplicate **resolution** stays 7d (**delivered there** — a deterministic group-token pass surfaces the pair and supersede/coexist/reject resolves it without ever deleting; ADR-0024); answering-from-facts stays 7c. **7b is now complete.**
>
> **Carried item (server-gating gap — log, don't fix here):** the new fact **verify** route is server-gated (`knowledge.edit`) as designed, but the existing **document** confirm route (`POST /admin/documents/{uuid}/confirm`) still sits in the open-admin block (UI-hidden via `canEdit`, not server-gated). Tightening it to server-side `knowledge.edit` to match is a real gap (same family as the Sprint-5 access findings) — see `sprint-07b-1/review.md` and `deploy.md`. Deferred to a follow-up; not changed in 7b-1.
- **Structured Reference Knowledge** (a third knowledge class — AI-segmented, human-verified, non-vectorized).
  - *The problem it solves.* Some source material is neither (a) a single-scope prose convenio (the PDF → chunk → embed path) nor (b) salary (`salary_tables` SQL). It is compiled, structured, often multi-scope content — e.g. a `.docx` or `.xlsx` that summarizes *periodo de prueba* (or any rule) across many convenios in one file (province → sector → rule). This breaks the one-document → one-convenio → one-scope model: there is no single `convenio_id` to assign, and serving it whole would risk cross-scope leakage (a Navarra employee getting an Álava rule) — the exact harm the architecture prevents.
  - *The model — the leaf is a scoped fact, not the file.* The AI reads the source (a whole file, part of a file, a `.docx`, or an `.xlsx`) and segments it into many small, individually-scoped records, each bound to a real convenio/territory/sector scope and a topic — e.g. {Álava, Ocio Educativo, Grupo 1} → *periodo de prueba* 5 meses; {Navarra, Hostelería, Grupo 1} → 90/75 días. One source file can produce many leaves; one leaf may come from part of a doc. This generalizes the salary pattern (salary was the first instance of "structured, scoped, queried-not-vectorized" data) to non-salary structured facts.
  - *Non-vectorized by design.* These records are structured rows, not `document_chunks` — exact, scoped, and queried (like salary), not embedded/similarity-searched. No vectorization step. This also means `.docx`/`.xlsx` non-salary content gets a home without needing the PDF chunk/embed pipeline to handle those formats.
  - *Audit-first: AI proposes, human verifies, then it answers.* Each AI-segmented record is created with `ai_agent` provenance and a `needs_review` state and is **not answerable** until a human verifies its scope and value (the same review discipline as ADR-0011 and the Sprint 3 bounded edit). On verify → it becomes answerable; the verification is logged (who/when) in the same append-only provenance history. The AI never makes a structured-fact record answerable on its own — a wrong scope-segmentation is a confident-but-wrong exact answer, the worst failure mode, so it must be gated by human review.
  - *Visible in the Knowledge Center as a distinct type.* Each record is a leaf in the lens hierarchy like any other, shown in both graph and list views, scoped under its convenio/territory/sector, with a distinct knowledge-type badge (a structured-reference type, visually separate from official convenio text and from `internal_hr_ruling`). Its card shows the scoped value, the link back to its source file/section, the topic, and the provenance + review-state timeline (AI-proposed → human-verified). Each record carries a source reference (origin file + location) so it is always traceable.
  - *Authority/precedence.* Slots into the existing authority model: it is reference/structured data, never silently overrides an official convenio for the same scope (re-escalate on conflict, consistent with the `internal_hr_ruling` rule).
  - *The upload agent's responsibility scope (what it does).* Read the file (the `.docx`/`.xlsx` the PDF pipeline can't open) → segment it into individual single-fact units → assign each fact its scope (bind to an existing convenio/territory/sector) → tag each (topic, validity/version, authority) → propose each as a `needs_review`, `ai_agent`-provenanced record linked back to its exact source location, with a confidence signal and explicit uncertainty where unsure. The AI does the high-volume tedious work (read/split/draft-the-scoping); the human does the low-volume judgement (confirm).
  - *The hard boundaries (what it must NOT do).* It does **not** make anything answerable (every record is inert until human-verified); does **not** create or rename vocabulary (assigns into existing vocabulary only — growth stays in `registry:import` / the propose-new-vocabulary path, ADR-0011); does **not** touch the answer engine (produces material upstream; the existing pipeline retrieves downstream — never in the live answer path); does **not** handle salary (wage tables go to `salary_tables` SQL — this agent is non-salary only); does **not** resolve conflicts or override (a fact that would contradict an official convenio is flagged for human adjudication, never silently published).
  - *Authority is proposed conservatively — err low, never high.* The agent does propose an authority level, but authority is a property of **provenance** (where the content came from / who made it), not readable from the text — a compiled summary may restate official rules while itself being only a hand-made reference. The safe default proposal is a reference/structured level (never `official_convenio` unless the source plainly warrants it): too-low is safe (it merely loses to a more authoritative source); too-high is the one mistake that can silently override the law. Authority is the tag most explicitly flagged for deliberate human confirmation. (Same spine as the `internal_hr_ruling` no-override rule and the Sprint-4 conflict gate: nothing the system generates internally — HR ruling or AI-extracted fact — can silently outrank official convenio law.)
  - *Central design risk = scope-assignment (and authority).* "Assign each fact its scope" is both essential and error-prone (real line: *Navarra, Hostelería, Grupo 2 (resto áreas): 60 días indefinidos / 45 temporales >3m / 30 temporales ≤3m* — province + sector + group + sub-group + three contract-type values in one line). A single wrong mapping is a confident, exact-looking wrong answer — worse than none. The human-review gate is not bureaucratic; it is the structural safety for the two tags (scope, authority) where a wrong guess does legal harm. Build effort and eval focus belong here.
  - *Canonical test fixtures (real hard cases, not toy data).* `PERIODOS_DE_PRUEBA.docx` and `PERI_ODOS_PRUEBA_ACTUALIZADOS_2026.docx` — non-salary prose rules, multi-scope (Álava + Navarra + … in one file; same job-group → different value by province), prose-not-table, and the two are different **versions** of each other (so the agent must get validity/version right: which is current). Plus `Tablas_acuerdo_parcial_Alhambra.xlsx` (SMI + hours + ranges — partly reference, partly salary-ish — a routing test). These exercise segmentation, per-scope assignment, version selection, and salary-vs-reference routing on real material. (Store these in the Sprint 7 fixtures folder when the sprint is built.)

### Sprint 7c — Multi-source composition layer (answer-engine change)  · size **M-L** · **DONE (ADR-0023)**
This is the ONE piece that touches the frozen 2b answer loop — teaching the engine to retrieve-and-merge across vectorized prose + exact structured facts with authority and grounding preserved. Maximum care, its own sprint, **after** 7b produces facts to compose. It is what makes the 7b investment pay off — without it, structured facts can't reach an answer.
- *Open architectural companion — the multi-source composition layer.* Producing these facts is only half the value; the answer engine must learn to consult several typed sources for one question and compose them safely — fetch a structured reference fact **and** a convenio passage, merge with authority preserved, ground the result. Today the engine answers from vectorized prose (+ salary as a separate exact path, + Sprint-4 rulings). Teaching it to retrieve-and-merge across vectorized prose + exact structured facts (authority + grounding intact) is a first-class part of this sprint's scope, not an afterthought — arguably the most important unbuilt piece of the whole answer model.
> **Built as one sprint in two internal phases under one review (ADR-0023), purely additive — a golden-trace regression pins prose + salary turns byte-for-byte to a pre-7c baseline after both phases.**
> - **7c Phase 1 — the routed reference-fact answer (the salary sibling).** A deterministic, no-LLM **pre-check** (`ReferenceFactRouter`) in the salary-pre-classifier layer (non-salary questions only; fail-safe fall-through) anchors the question on the **existing** topic lexicon (relocated to a shared `TopicLexicon`) and routes to `ReferenceFactAnswerService` (mirrors `SalaryAnswerService`) **only** when a `verified`, in-scope, in-validity fact exists. It quotes the exact `value`/`raw_values` verbatim, cites the source with `chunk_id = null` at `structured_reference`, and **skips `/ground`**. Only-verified is the spine (an unverified/out-of-validity/future-only fact escalates the new `reference_fact_coverage_gap`); scope is most-specific-else-escalate (never guess a group); two verified matches → most-recent validity else escalate (resolution is 7d). Independently shippable.
> - **7c Phase 2 — the fact·convenio-prose merge.** When governing convenio prose on the topic is also present (clears Check A), the fact is handed to `/synthesise`+`/ground` as **one more typed, authority-labelled source** (`source_type = reference_fact`, `chunk_id = null`, `structured_reference` ranked **below** `official_convenio`) — reused, never rewritten (an additive nullable-`chunk_id` + `source_type` field). The composed answer is **generated** so it **must `/ground`**; the convenio always governs and a same-point conflict escalates (`conflict`) **before** synthesis, never blends. One schema migration in all of 7c (`reference_fact_coverage_gap`); Phase 2 adds none.

### Sprint 7d — Semantic conflict detection + fact version resolution + succession proposal  · size **M** · **DONE (ADR-0024)**
**Built:** one additive read-only comparison primitive (`hr-ai POST /compare-scope`, `authority_level` filtered **in the SQL** so the block decision is k-independent) and three human-adjudicated surfaces on top of it — **(A)** the semantic publish fence, added as `existing_block OR semantic_block` so it can only turn ALLOW→BLOCK (the Sprint-4/Correction-01 fence is untouched and still the first term), with two bands (block / **explicit acknowledgement**) and **no path from a failed comparison to a clean publish**; the §8.5 reverse re-check at the flag-only bar (a newly-active convenio opens a `conflict` task on an overlapping ruling and **never** changes `retrieval_status`); **(B)** fact **version resolution** — a deterministic group-digit-token duplicate pass (no embeddings, no threshold) plus supersede / coexist / reject, where supersede **closes the older fact's validity and never deletes**, and resolution reaches chat through corrected **data only**; **(C)** the inert **AI succession proposal**, where `successor` requires overlap **AND** strictly-later validity (a conjunction, so a similarity opinion alone cannot produce the one label that would tempt a human to retire a live document), written as three columns on the review task and confirmed through the unchanged 7a write-side. Block thresholds are **code config only** — for a block-triggering similarity threshold, *lowering* blocks more, so ADR-0019's `max(floor, admin)` would let an admin loosen the fence. See `sprints/sprint-07d/review.md`.

**Follow-ups recorded (not built):**
- **Exposing the semantic thresholds in the Sprint-6 guardrails UI must combine with `min(baseline, admin)`, never `max`** (ADR-0019's direction is inverted for a block-triggering threshold). It also needs additive `guardrail_configs` columns. A Sprint-6-family follow-up; until then the values live in `config/hr.php`.
- ~~The thresholds ship PROVISIONAL~~ — **calibrated on the staging corpus** (`fence:calibrate-semantic --json`, real rulings + the labeled anchors in `sprints/sprint-07d/eval/anchors.json`): `semantic_conflict_threshold = 0.78`, `semantic_review_band = 0.66`, committed to `config/hr.php` with the justifying scores. `succession:gold-eval` ran with **0 confidently-wrong successors on 5 real, SQL-confirmed pairs** (`succession-gold.json` rebuilt from the provisional placeholders); both succession thresholds measured and left unchanged (the strictly-later-validity conjunct, not the score, is what separates true successors from a duplicate ingest that scored higher than a real successor). Full numbers, live-fence proof, and the caveat below in `sprints/sprint-07d/review.md` §2.
- **The calibration surfaced two corpus-state gaps that are load-bearing before this is live in production — tracked as go-live items, not code work (`deploy.md` §4b):** (i) **no document in the corpus carries a topic tag**, so the Sprint-5 structural term blocks every ruling publish before the semantic term is ever consulted — the flywheel is built and calibrated but not yet usable end-to-end until convenios are tagged (7a's proposal pipeline + human verify); (ii) the anchor set is **n = 2 per class**, and the two classes already **overlap in score space** (a same-topic-other-point anchor scored 0.8751, higher than either near-verbatim paraphrase anchor at 0.80/0.81) — at n = 2 this over-blocks gap-filling rulings by construction, and the fix is more anchors + probe-level scoring as the tagged corpus grows, not a higher threshold.
- **The acknowledgement band is the thing to watch.** Every acknowledged publish is audited with its scores (`escalation_events.publish_acknowledged_overlap` + `detail`); if it is routinely clicked through, the band — not the block — needs adjusting.
- **Question-granular knowledge growth is NOT built** (see the original scope note below): 7d evolved the *fence* to meaning, but the flywheel still attaches a resolved escalation as a whole ruling document, not as a question-granular entry.

*Original scope note (kept for intent):* replaces the topic-tag conflict fence (patched fail-closed in Sprint-5 Correction-01) with meaning-based comparison; touches the publish/safety path; self-contained; independent of 7a–c.
- *Inherited from 7a:* the **AI succession-proposal** (suggesting that a near-expiry document is succeeded/sibling/conflicted by another, by comparing meaning). 7a deferred this here because it needs the same semantic-comparison machinery 7d builds; 7a wired only the human-confirmed write-side (the expiry queue + `predecessor_document_id`, same-convenio, never auto-retire). 7d reuses 7a's "inert until verified" spine (ADR-0020) — any AI succession suggestion is a proposal a human confirms.
- *Semantic conflict detection + question-granular knowledge growth (evolve from topic-tags to meaning).* The Sprint-4 publish conflict fence currently keys on topic-tag match (does an active `official_convenio` in the asker's scope share the ruling's topic). This is a blunt proxy and is doubly limited today: topics are sparsely populated (the empty-honest topic lens — `document_topics` is mostly empty), so the fence largely runs in its conservative scope-only fallback; and even fully tagged, a fixed topic list can't represent the real granularity of questions (within one topic like *vacaciones* people ask ten distinct things — carry-over, payment, denial, part-time, sickness-during…). Evolve the fence to compare meaning — embed the ruling and search the scope's `official_convenio` chunks for semantically-close passages — so it asks *"does the official text already address this specific thing?"* rather than *"is there any convenio on this topic?"*. The same mechanism lets the flywheel grow knowledge at question-granularity: each resolved escalation attaches as its own precisely-placed entry, matched to future askers by meaning regardless of wording. This reuses the existing embedding machinery (the answer engine already embeds chunks and finds nearest-meaning; the Sprint-4 rulings already get embedded and enter retrieval) — it points that machinery at the publish/conflict + flywheel-growth jobs. *Safety tuning:* when similarity drives the fence (a safety gate), fuzziness must fail toward caution — flag-for-human on plausible overlap, never silently decide "no conflict." For growing the base, fuzziness is cheap; for blocking an override of the law, it must err toward human review. *Scope note:* this governs the `internal_hr_ruling` publish/conflict path — it does not add any blocking to normal document ingestion (`official_convenio` PDFs/docs are never blocked by this fence; they version, see succession note above).

### Sprint 7e — OCR for scanned documents  · size **S–M** · **DONE** (ADR-0026)
OCR is a **format problem** (a scan has no text layer), distinct from 7b's scope problem (a multi-scope file doesn't fit the single-`convenio_id` model) — kept as its own sprint so a scan-extraction change can't destabilise the structured-knowledge work of 7b, and vice versa. One risk at a time.

> **Built and merged to `main`** (Step 1 — eval + engine decision; Step 2 — integration; Step 3 — backfill; Step 4 — acceptance test; ADR-0026, `sprints/sprint-07e/review.md`). A measured eval (5 fixtures, human-corrected gold, `score_ocr.py`) picked **Claude vision, `claude-opus-5`** over Tesseract (Tesseract fails table structure outright — a real, disqualifying category) and two older Claude models, by the decision rule (column integrity → header survival → table cells → eu WER → CER → cost). Integration: `/extract` marks a text-less page `ocr_pending` (within a page cap) but never calls the OCR model inline — a queued job (hr-backend's `OcrDocumentPages`/`OcrPage`, mirroring `ProposeDocumentTags`) drives the per-page `POST /ocr-page` call and writes `document_pages.text`/`extraction_source`/`ocr_quality`/`ocr_engine`/`ocr_cost_usd`/`ocr_bilingual`; a new hr-ai S3 sidecar (`documents/{uuid}/ocr/{page}.json`) is what actually reaches `/embed`'s chunking pipeline (`document_pages.text` alone is a no-op for chunking — `/embed` re-extracts from the original PDF and never reads that table). OCR'd documents stay `under_review`/0-chunks until a human `confirm()`s — no exception, proven by test (`Sprint7eOcrInvariantTest`, plus hr-ai's own `ocr_sidecar_test.py`); the golden-trace regression (`Sprint7cAdditivityRegressionTest`) stays green. `documents:ingest-folder --ocr` (opt-in, page-capped) and `documents:ocr-backfill` are both built and were **run for real on staging: 106 pages OCR'd** across the 9-document inventory. Pedram human-verified docs 31 and 50 against the source images (two-column layout, `eu` checked against `es`, numbers matched) — both correctly remain `under_review` (`NO_CONVENIO_MATCH`, a registry item, not an OCR defect). **Acceptance test** on a document that *does* bind cleanly (31/50 don't): doc 18 bound to convenio 13 (Limpieza Edificios y Locales, Gipuzkoa — its own OCR'd text cites the exact registry code), embedded (170 chunks, es=91/eu=79), and a scoped test employee got a fully grounded (8/8 claims) cited answer — surfaced from the pre-existing **active duplicate** doc 13, not doc 18 itself (`chunks:embed` also embeds `historical` docs for ADR-0024's semantic-fence comparison, but the live employee path only ever retrieves `active` ones); doc 18's own OCR'd chunk was separately proven correct via the admin single-document sandbox. Also produced `corpus-coverage.md`, a full-corpus answerability ledger (baseline: 36/101 docs answerable, 10/26 convenios a full gap) — the Sprint-8 coverage-gap map in embryo. Three follow-ups intentionally left for a human/later sprint, not auto-resolved — see `deploy.md`'s go-live list: doc 50/85's convenio binding (a registry decision), reference-source fixtures never ingested on staging (0 `reference_facts` rows there), and doc 14's title carrying the original client's codename.
>
> **The bilingual-verification rule ended up simpler than originally scoped.** Round 1 of the model eval found `claude-sonnet-4-5` measurably worse on Basque than Spanish (WER ≈ 22–24%, a >25x relative gap vs. Spanish on the same fixture) — which would have justified the separate "verify the Basque column" affordance this section originally called for. Round 2's model choice, `claude-opus-5`, measured that gap essentially disappearing (mean eu WER 0.71%, well under 1%), so the separate gate was **not built** — both language streams of a bilingual OCR'd page ride the identical `under_review → confirm → embed` path; a reviewer-guidance note (not a second approval step) is kept instead. See ADR-0026 for the full numbers.

> **Follow-up: the salary-PDF path, and the correction it exposed.** OCR made salary tables that only ever existed as PDFs importable for the first time (`salary:pdf-to-xlsx`, `corpus-coverage.md`'s flow 2b): the OCR'd grid is written verbatim into a **derived** `.xlsx` linked by `derived_from_document_id`, a human reviews it against the page image and approves a header-row mapping (gazette headers carry units, and `salary.py` matches exactly), then the **unmodified** `salary:import` runs and `--mark-provenance` stamps `salary_tables.source = 'ocr_pdf'` and restores each mapped column's original header text into `raw_values`. Four human gates; no cell is ever guessed. Live on staging for convenio 15 (docs 12/27) and convenio 3 (doc 57, whose year-column grid needed the separate `--year-columns` transform). Reading a printed monthly next to a stored one for the first time then exposed a Sprint-2a defect: `salary.py` had been computing `base_salary_monthly = gross_annual / 14` for every row, which contradicted the source on **21 rows across convenios 10, 15 and 22** (up to +36 %) and was right only for the one convenio that pays in 14. **Correction-salary-01 / ADR-0027** removed the derivation entirely — a typed salary figure is now a source cell or it is NULL, `pagas_count` replaces `num_payments` and never divides anything, `SalaryAnswerService` omits a monthly it does not have, `salary:import` fails loudly on a recognized-but-empty grid, and `salary:audit-monthly` is the standing guard. See `sprints/sprint-07e/review.md` §5–§7.

**Go-live relevance.** OCR'd text alone doesn't make a document answerable — a scan's *scope* still has to resolve before `chunks:embed` will touch it. All 9 of the 7e backfill's documents now have real, human-quality OCR'd text, but only doc 18 (the acceptance test above) is actually bound + embedded; the rest — including active-scope `PACTO CULTURA NAVARRA` (id 31) and `CONVENIO DEPORTE NAVARRA 2025–2028` (id 50) — remain `under_review` on a registry/scope gap, unrelated to OCR quality. **COEAS Estatal was never one of the 9** — it already has a full text layer; its earlier appearance on this list was a mistake, corrected in `deploy.md` §4b/§5. *Cross-ref: `deploy.md`'s go-live list (the doc 50/85 binding decision, the reference-source ingest gap, doc 14's codename); `corpus-coverage.md` (the full 101-document picture); Sprint 7b (the other "can't answer from this" class — scope, not format).*

### Sprint 7f — Structured group-scope modeling (the proper fix for group-scoped fact answering)  · size **M** · **DONE** (2026-09-09, ADR-0028)

**Built as specified, with two things the plan did not anticipate.** The tree, the join table, the propose-then-approve flow and the exact matcher all landed as written; `factMatchesGroup` and `resolveEmployeeGroupCode` are deleted and the answer path no longer reads `group_code`. Live on staging: one employee, one question, three nodes — *resto áreas* → 60/45/30, Grupo 1 → 90/75/60 (the compound fact's other binding), Grupo 2 (split parent) → escalate. Group eval **31/31 exact, zero under-splits, zero over-splits** across six convenios for $0.95. 309 backend tests green; the 7c golden trace and baseline check A byte-for-byte unchanged.

The two discoveries worth carrying forward: (1) **binding is a decision separate from approval** — the first cut made the approval diff the only moment a fact could be bound, which made a reviewer's first pass final; and (2) **a label the parser must refuse can still be knowable to a human**, so binding has an explicit override lane that files the decision as *asserted* rather than *read* (`facet = group_scope_manual`, planner's refusal reason kept beside the human's note). Both surfaced only because a real reviewer worked the real surface at Checkpoint 2. Full account in `sprints/sprint-07f/review.md`; the escalation-explanation follow-on is **Sprint 7g**, deliberately not in 7f.

**Original entry, kept for the record of why it was built this way:**

The proper fix for a **latent group-resolution bug** surfaced during the 7c eyes-on: a reference fact can be scoped to a group whose distinction lives only as **prose inside the `group_label` string** (e.g. convenio 21 Hostelería Navarra: fact 202 *"Grupo 1 (todas las áreas) y Grupo 2 (área 5)"* = 90/75/60 días **vs** fact 203 *"Grupo 2 (resto áreas)"* = 60/45/30 días — the convenio splits Grupo 2 into two different probation values). The 7c group-matcher (`ReferenceFactAnswerService::factMatchesGroup`) keys off a **bare-digit token** in that prose, so an employee with `group_code = "2"` matches *"Grupo 2 (área 5)"* and would be told 90/75/60 — a **confident, verified, exact wrong-group answer** (the same failure class as the 7b-2 cross-province trap, the harm the whole architecture exists to prevent). The digit qualifier can't represent "área 5 of Grupo 2"; the real Grupo-2 fact (203) is `needs_review` and never competes.

**Why it is latent (not live) today, and why no quick patch was taken:** the hole only fires when a group-scoped fact is **verified** *and* an employee has a **resolvable `group_code`**. Convenio 21 has **zero `convenio_job_categories`** (no salary `.xlsx` to mint them — `group_code` is only ever populated by `salary:import`, and across the corpus it is decimals/prose/null, never a clean digit), so no Hostelería employee resolves a group and the mis-match path is unreachable with current data. The decision (recorded here) was to **skip the interim fail-safe patch and fix the model properly** — **on the explicit condition that this entry gates it: group-scoped facts must not become answerable for any convenio until 7f exists.** ⚠️ **Hard precondition — do not seed `convenio_job_categories` with clean digit `group_code`s for a convenio that has verified group-scoped facts (e.g. convenio 21) before 7f lands** — doing so makes the latent wrong-answer path live. (If group-scoped answering is needed sooner, the interim safe move is the fail-safe matcher: a *qualified/compound* `group_label` — any "área"/"excepto"/"resto"/compound "y" — **escalates `reference_fact_coverage_gap` instead of digit-matching**; tightening-only, escalate-more-never-answer-more. Recorded as the available stop-gap, not built.)

**The proper fix — model group scope as structured data, populated by AI-propose-then-human-verify (placing the "let the AI suggest groups" instinct on the safe side of the determinism line):**
- *Build-time (AI proposes, human verifies — the ADR-0011/0020 spine).* An agent reads the convenio text and **proposes** the convenio's real group structure **at the granularity the convenio itself assigns different values** — undivided where the convenio is undivided (most groups: a clean "Grupo N", the simple one-level case), split only where the convenio splits a group into different values (e.g. Grupo 2 → *área 5* vs *resto áreas*, because they carry different probation figures). A human approves; only then do the groups/subgroups exist. The AI **proposes** scope structure (incl. groups that don't yet exist); it never mints silently and never decides scope at answer time.
- *Structured representation.* Replace prose-in-`group_label` with a real, matchable group/sub-area scope on both the **fact** and the **employee** (the employee profile gains a structured group assignment; today it carries only `convenio_id`/`territory_id`/`job_category_id`, with group reachable only indirectly via `job_category.group_code`). The fact's group scope and the employee's group scope compare **exactly**, not by digit-substring.
- *Answer-time stays deterministic (no AI in the legal-weight decision — ADR-0015/0016).* The matcher does an exact, auditable comparison on the approved structured scope: undivided group → match on the group (the simple one-level case, preserved); split group → the sub-area distinguishes them, so no wrong match; can't resolve which sub-area the employee is in → **escalate** (the safe fallback already in `ReferenceFactAnswerService`). The AI builds the scope map; deterministic code reads it.

**Why AI semantic matching at *answer* time was rejected (recorded so it isn't re-proposed):** handing "which group does this employee belong to" to the model at answer time hands the single most legally-dangerous judgment to the component deliberately kept out of it (ADR-0015/0016) — a confident "close enough, área 5 matches" is the exact confident-wrong-scope harm, with *more* confidence and *less* auditability than the digit bug. AI belongs on the **build** side (propose structure), never the **answer** side (decide who matches).

**Out of scope / boundaries:** reuses the 7a/7b-2 propose-then-verify machinery and the existing escalate-don't-guess fallback; does **not** touch the frozen answer loop beyond the matcher's scope comparison (which already exists — it gains structured inputs, not new authority); no change to prose/salary paths. Cross-ref: `sprint-07c/review.md` (where the eyes-on surfaced it), `ReferenceFactAnswerService::factMatchesGroup` (the digit matcher to replace), ADR-0021/0022 (the fact scope model this extends).

### Sprint 7g — Escalation explanations + review-surface hardening + three operational fixes  · size **S–M** · **DONE** (2026-09-10, ADR-0029)

The follow-on that makes Sprint 7 usable, not another answer-engine change: none of the six items touch when or whether anything escalates, retrieves, synthesises, grounds, or answers — `Sprint7cAdditivityRegressionTest` and the 7c/7f reference-fact suites stayed green after every item, on every repo. Full per-item build detail and staging proof: `sprints/sprint-07g/review.md`.

- **Item 0 — deploy keys, then private repos (security, merged first and separately, on `main`).** Closed the credential gap Sprint 7f's close flagged as "the shape of the answer, not a decision taken": four read-only, per-repo GitHub deploy keys installed on the staging box, `deploy.sh`/`deploy-run.sh` switched from anonymous HTTPS to the aliased SSH URLs, verified green both before and after Pedram flipped all four repos to private. No PAT ever created or considered. Full build record: `deploy.md` §7 Session 5.
- **Item 1 — escalation explanations (ADR-0029).** The employee side narrows to **one fixed neutral message** across every escalation reason, asserted by a full-enum scan test. HR gains a **deterministic** per-reason fact registry (`EscalationExplainer`, a pure function over the trace, 39-entry matrix, guard-tested for completeness) plus a cheap Haiku **"Resumen IA"** paragraph restating those facts only — verified by a no-new-claims check, falling back to deterministic sentences on any failure — with the fix action/surface/deep-link always the deterministic values, never the model's. Five additive columns on `escalation_cards`; hr-ai gains no new endpoint (reuses `/synthesise` via `ROUTER_MODEL`, the same key path `/route` already uses). See `architecture.md` §8.6, `data-model.md` §9.
- **Item 2 — Review surfaces at real corpus size.** Pagination + a visible total on all four Review tabs (mirroring the 7e Documents-page pager); the two tabs that didn't already paginate server-side (Vocabulary proposals, Expiry) now do. The Reference-facts list and the Groups tab's fact lists show each fact's numeric id + the first line of its `source_excerpt` inline. The `#doc=<uuid>` deep-link pattern generalises to `#view=<view>&tab=<tab>&fact=<uuid>&emp=<uuid>&convenio=<id>` — the scheme Item 1's fix links depend on.
- **Item 3 (F-1) — checksum-dedupe no longer silently re-types a document.** A content-hash match on re-ingest now mirrors the manual-edit scope gate exactly: a scope-affecting change on a dedupe hit reports "already exists" and changes nothing unless explicitly confirmed (`confirm_scope_change` / `--retype`), which then records an `admin_manual` audit row.
- **Item 4 (F-2) — binding-aware `facts:scan-duplicates`.** When both facts in a candidate pair have a bound group node (ADR-0028), the comparison uses the bound nodes (same/ancestor/descendant → candidate; siblings → not a duplicate), falling back to the pre-7g token pass only for unbound facts. Staging dry-run: 10 pairs → 9 — the three sibling-crossing false positives (Grupo 2's split área-5/resto-áreas nodes) resolved into the two true same-node pairs the old algorithm had missed, the real Navarra Acción e Intervención Social pair (and every other unbound-fact pair) unaffected.
- **Item 5 (F-3) — `otp.sh` no longer extracts a Message-ID fragment as the login code.** Re-anchored on the mailable's own OTP markup line, tracking per-recipient records to select the newest code. Verified end-to-end on real staging (request → log read → verify → real bearer token).

**Docs at close:** `architecture.md` §8.6 (escalation explanations) + the §2 deploy-key note (Item 0, already landed with Session 5); `data-model.md` §9 (the five `escalation_cards` columns); `deploy.md` §7 Session 6 (Items 1–5 build record, including the dedupe-guard behaviour change and the Review-pagination note) alongside Session 5 (Item 0); **ADR-0029**; this entry; `sprints/sprint-07g/review.md`.

### Sprint 8 — Analytics + coverage gaps + quality sampling  · size **M**  · **DONE** (2026-09-10, ADR-0030) — *pending user review, not merged*
The HR-facing measurement layer.
- Deflection rate, top questions, unanswered questions, escalation volume by topic, satisfaction.
- **Coverage-gap detection** (province × sector with employees but no current document — free from the faceted model). *(Sprint 2c surfaced concrete instances to seed this: registry-expired convenios with **no loaded active successor** — Andalucía COEAS, Vizcaya Intervención Social, Huesca Hostelería, Salamanca Oficinas, Gipuzkoa Intervención Social, Navarra Hostelería/Oficinas-prose, Deporte Estatal — plus the scanned-no-text actives, see `deploy.md` §5.)*
- **Monthly accuracy sampling**: a random sample of conversations reviewed by HR — the quality-audit mechanism that catches drift without re-checking everything.
- **Follow-up — re-chunk the Estatuto (national law, id 73) on article boundaries (ADR-0017 splitter).** Held out of Sprint 2c because it is the **universal baseline**: re-chunking it perturbs the convenio-vs-baseline precedence balance just stabilised in 2b-2 (the broadest, most sensitive blast radius). **Home here** because corpus-wide answer-quality data will show whether the Estatuto's current (packed) chunks actually cause any wrong/weak-precedence answers on the national-law side. **Jumps the queue** if it causes a visible wrong answer on the national-law side before then (the same trigger that moved the convenio re-chunk up). Uses 2c's proven splitter; **gated on the national-law gold tests** (especially *trabajo a distancia → national_law*). **Decision (re-evidenced on real staging traffic, 2026-09-10): not re-chunked.** The evidence query (§8.1's `authority_used`-national-law-only-on-active-convenio-topics count), re-run against real `hr-staging` `message_traces`, found exactly **1** national-law-only turn — against convenio 28, which is the `DEV-FIXTURE-0001` placeholder convenio (0 real content, not a real employee's real convenio). Per §8.2's rule, this is the "convenio genuinely silent, national law correctly fills the gap" case, not the "convenio speaks but got overridden" case — so no re-chunk was triggered, doc 73 remains untouched. This supersedes the earlier local-sandbox "no evidence, not done" finding with a stronger result: real traffic exists, and the specific trigger condition still has zero real occurrences. Full write-up: `sprints/sprint-08/review.md` §9/§13, ADR-0030.

Built: a Laravel scheduler (`withSchedule()` + a `scheduler` compose service), `CorpusCoverageService` (the coverage grid + `corpus:coverage` export + a gaps-closed snapshot), `DeflectionAnalytics`/`analytics_daily_rollups` + `stats:*` commands, escalations-by-fix (backfilled + a grouped read over the 7g explanation matrix), question clustering (`POST /embed-batch` on hr-ai, τ=0.80 — calibrated on staging against a real 20-pair set, classes overlap, kept at 0.80 as a named config value, medoid labels, min/max logged), monthly quality sampling (`quality:sample`, a review screen, wrong→task via the ADR-0029 machinery), three new admin screens (*Analítica*, *Cobertura*, *Calidad*) gated by a new `analytics.view` ability, and (shipped, not cut) `message_feedback` — a thumbs signal in chat feeding one satisfaction tile, read-only and never fed back into the answer loop. Full account, the real-vs-sandbox-counts caveat, and per-step proof: `sprints/sprint-08/review.md`. Design record: **ADR-0030**.

### Sprint 9 — Privacy / GDPR hardening  · size **M**  · *recommended as a real sprint*
The dedicated pass for what's threaded earlier (pillar §4.2). Builds on **ADR-0018** — the role-scoped access model + `conversation_access_log` start in Sprint 5; Sprint 9 hardens them.
- Encryption-at-rest for chat data, **retention limits + erasure** (incl. over `conversation_access_log`), and the **audit/reporting layer over `conversation_access_log`** (who-viewed-whom dashboards over the Sprint-5 log).
- AEPD considerations; **comité de empresa (works council) sign-off** preparation — which can *gate deployment*, which is why this is a real sprint and not an afterthought. ADR-0018's "broad access is a granted ability + every access is logged + the server is the boundary" is the spine of that sign-off.

### Pre-go-live phase — Operational "between" layer  · size **M–L**  · *the run-it-for-real last mile*
Not a feature sprint; the work that makes it safe to run for 1,500 people unattended (your "between the two extremes" call).
- Monitoring + alerting (know when a service is down or escalation rates spike), rate limits (abuse/cost protection), backups + a tested restore, deployment, app containerisation.
- Deliberately **defers** heavy enterprise polish (deep dashboards beyond Sprint 8, advanced guardrail tooling) to a later phase once it's live and proven.

---

## 4. Cross-cutting pillars (built *into* sprints, not standalone)

**4.1 Auditable history & the trace.** Built into **2b** from the first answer — every chat turn and its structured trace stored permanently, searchable, reviewable. Not a late add; retrofitting "log everything auditably" is painful, so it's a property of the chat from day one. Doubles as the eval dataset.

**4.2 Privacy / GDPR.** Threaded early (role-scoped conversation access designed into **2b** and enforced in **5**) **and** given a dedicated hardening pass (**Sprint 9**). GDPR can't be bolted on at the end — but it also earns a real sprint because AEPD / comité-de-empresa can gate go-live.

**4.3 The learning-loop / flywheel.** The "system gets smarter with use" pillar you emphasised. Its core is **Sprint 4** (resolved escalation → human-approved, scoped knowledge article). It's *fed* by auditable history (4.1) and *measured* by analytics + coverage gaps (Sprint 8). Realised across those sprints, not isolated in one — but it is a first-class pillar, not a footnote.

---

## 5. The count, honestly

Two sprints are **done** (0, 1) plus the near-complete design system. Ahead: **2a, 2b, 3, 4, 5, 6, 7, 8, 9, and the pre-go-live phase** — roughly **ten more units of work**, so the full two-sided, production-ready platform is on the order of **11–12 sprints plus a pre-go-live phase**.

This is higher than the early "~6" figure, and that's the honest correction: the early number predated every close-up scoping pass. It grew because we **split overloaded sprints rather than cram them** (Sprint 1; RAG into 2a/2b), inserted the design system, and gave the flywheel, privacy, and operational work **real homes instead of footnotes**. None of that is scope creep — it's the scope becoming visible.

Where *your* "done" line sits is a real choice: a usable, accurate, auditable platform (employee chat + admin console + flywheel) is reached around **Sprint 5–6**; everything after that is making it measurable (8), compliant (9), and operable at scale (pre-go-live). The "between" posture you chose means we build through the operational layer but **defer** deep-maturity polish — so the realistic target is the full sequence above **minus** the heavy enterprise extras, which stay parked until it's live and proven.

---

## 6. Two sequencing calls (recommended here — flip either if you disagree)

1. **Privacy/GDPR = a real sprint (Sprint 9), not just a phase-2 section.** Recommended because works-council sign-off can gate deployment, and role-scoped access must be designed in earlier regardless. *Flip:* fold the hardening into the pre-go-live phase if Sedena's compliance path turns out lighter.
2. **The learning loop = realised across Sprint 4 (core) + 8 (measurement), not a single isolated sprint.** Recommended because its pieces naturally live in the escalation board and analytics. *Flip:* if you want a dedicated "knowledge-gap mining" surface beyond what Sprint 4/8 give, that becomes its own sprint.

---

## 7. Open items still parked (on record, not lost)

- One person being **both admin and employee** (Sprint 0 non-blocker) — matters when employee identity meets the chat (Sprint 2b); decide there.
- **Bilingual chunking** for Euskara content — handled as part of 2a's chunking rule (don't blend languages); deeper eu handling can be revisited if retrieval quality needs it.
- **Scope precedence** between territory levels (does a provincial convenio override a regional one?) — deferred from Sprint 1; decide when retrieval eligibility is built (2a/2b).
- **Agentic multi-turn clarifier (parked in 2b-2)** — the salary category disambiguation is single-turn (a constrained pick from the convenio's categories). A fuller multi-turn clarifier (free-form back-and-forth to resolve ambiguous scope, e.g. multiple plausible categories, unclear as-of date, compound questions that need narrowing) is deferred; the single-turn pick covers the common case without the conversational-state complexity.
- **2a wage-table-chunk cleanup (still parked)** — exclude wage-table pages from prose chunking at ingest (ADR-0006: salary is never a vector chunk). Salary routing + the table-aware grounding gate mitigate the residual; the ingest-time fix is not yet done.
- **✅ Article-boundary re-chunking of active prose convenios — DONE (Sprint 2c, ADR-0017).** Each **Artículo N.º is now its own chunk**; cross-article packing removed. The buried-grant artifact is fixed at the substrate: Navarra Art. 9.º Vacaciones (*"37 días laborables"*) is now a clean, topically-led chunk that ranks into the synthesis cap on its **own raw score, before** the precedence boost (verified de-crutch trace, `sprint-02c-rechunk/review.md`). **Correction-03's precedence re-rank is reclassified from interim load-bearing to defence-in-depth** (kept; no answer-loop change) — **the Correction-03 residual is closed.** Re-chunked the **registry-active** convenio set (12 docs); regression held (no body-text loss, bilingual eu/es split intact, counts sane). The **Estatuto** (national law, id 73) was deliberately **held out** → tracked as a Sprint 8 follow-up below.
- **2a wage-table / salary-table-PDF mistag (surfaced in 2c)** — the registry has **id 94** (`OFICINAS Y DESPACHOS NAVARRA_Tabla 2025`) typed `convenio_text` and `active`, but it is a **salary-table PDF** (4 table chunks, no articles). It was **excluded** from the 2c re-chunk per ADR-0006 (salary is SQL); it should be **retagged** to `salary_tables` (or routed via SQL). Same family as the parked wage-table-chunk cleanup above.
- **Symmetric convenio-side conflict re-check (Sprint 4 Q-F residual)** — the no-silent-override fence runs at **ruling-publish** time. The reverse direction — when an `official_convenio` is added or edited, re-scan **already-published `internal_hr_ruling`s** in that scope+topic for newly-introduced conflicts (demote / re-escalate) — is **deferred to a later precedence/guardrails sprint**. Largely **latent** until a self-service convenio-upload path exists; 2b retrieval precedence stays frozen (a ruling is treated as governing, like a convenio). Recorded as a known boundary in `architecture.md §8.5`.
- **Restrict `convert` by escalation reason (Sprint 4 → Sprint 6)** — Save-as-knowledge is the agent's judgment this sprint (optional, not reason-gated). Whether certain escalation reasons should forbid conversion is a Sprint-6 guardrails-policy matter.
- **Sedena branding** — neutral-blue placeholder accent in one token (ADR-0012); swap when a brand colour is chosen.
